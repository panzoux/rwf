# Phase 7.7 — スマート・トラッシュ（ゴミ箱）管理

**作成日**: 2026-06-01  
**状態**: 実装中（2026-08-02 更新。バックエンド/Job/Action層はTask 1-13、タスクパネル連携は
Task 14で完了。残りはTask 15以降。詳細は下記「6. 実装タスク一覧」）  
**対象**: 安全なファイル削除、削除済みファイルの復元

---

## 1. 目的

「削除」操作を物理的な抹消ではなく「ゴミ箱への移動」とすることで、誤操作によるデータ損失を防ぎ、Phase 7.6 のロールバック機能と連携して「削除の取り消し」を実現する。

---

## 2. プラットフォーム別戦略

OSごとの作法が異なるため、以下の優先順位で実装する：

### **Windows**
- `Shell32.dll` API を利用し、OS標準の「ごみ箱」へ移動。
- ユーザーはデスクトップのごみ箱から手動復元も可能。

### **macOS**
- `NSFileManager` (Cocoa API) を利用し、OS標準の「ゴミ箱」へ移動。

### **Linux**
1. **FreeDesktop.org Trash Specification**: `~/.local/share/Trash` 等の標準フォルダを優先。
2. **trash-cli**: 外部ツールが利用可能な場合は活用。
3. **独自管理 (Fallback)**: 非標準環境や特殊なマウントポイントでは、そのボリュームのルートに `.rwf-trash` を作成し、メタデータ（元のパス、削除日時）と共に管理。

---

## 3. 基本仕様

- **同一ボリューム維持**: ゴミ箱への移動は、可能な限り同一ファイルシステム内での `Rename` (Move) として実行し、巨大ファイルの「コピーによる遅延」を避ける。
- **メタデータの保存**: Linux Fallback 方式や独自管理の場合、復元のために元のフルパス情報を保持する。
- **ゴミ箱を空にする**: RWF 上から「ゴミ箱を空にする」コマンドを提供（全プラットフォーム共通）。
- **自動削除**: 一定期間（例: 30日）経過したゴミ箱内のファイルを自動削除する設定項目。

---

## 4. UI/UX

- **削除確認**: config により「即座にゴミ箱へ送る」か「確認ダイアログを出す」かを選択可能。
- **物理削除 (Permanent Delete)**: `Shift + Delete` でゴミ箱を経由しない物理削除をサポート。
- **視覚的フィードバック**: ゴミ箱へ移動した際はタスクパネルへ通知。

---

## 5. ロールバック (Phase 7.6) との連携

- 「削除の Undo」が実行された場合、ゴミ箱から元のパスへファイルを戻す処理を `InverseJobSpec` として実行する。
- この連携自体は Phase 7.6 側の実装範囲。7.7 のスコープは「Undo が呼び出せる `RestoreFromTrash` Job
  を提供すること」まで（下記 Task 9 で完了済み）。

---

## 6. 実装タスク一覧

worktree `worktree-phase-7.7-smart-trash`（`.claude/worktrees/phase-7.7-smart-trash`）で作業中。
ブランチは `main` から16コミット進行、作業ツリーはクリーン（2026-08-02 OSクラッシュ後に確認済み、
未コミットの作業は無し）。

### 完了（Task 1-13）

- [x] Task 1: `trash` crate 依存追加、`TrashRecord`/`TrashLocation`/`EmptyTrashScope` モデル型
      (`9b73299`)
- [x] Task 2: `move_to_trash_sync`（OS管理ゴミ箱 + `.rwf-trash` fallback + `force_fallback`）
      (`1132728`, `4774f43`, `f48eafb`, `1cf2f96`)
- [x] Task 3: `.rwf-trash` fallback 層（move/restore） (`d496184`)
- [x] Task 4: `restore_from_trash_sync`（OS管理 + fallback 両対応） (`801319e`)
- [x] Task 5: `purge_os_trash_sync` / `purge_fallback_dirs_sync` (`692f1af`)
- [x] Task 6: `FilesystemBackend::move_to_trash/restore_from_trash/empty_trash` (`7c47c5a`)
- [x] Task 7: `JobKind::MoveToTrash/RestoreFromTrash/EmptyTrash` + `SuccessData` variants
      (`279b00c`, `71b03e8`)
- [x] Task 8: `execute_move_to_trash/execute_restore_from_trash/execute_empty_trash`
      （job_executor 実行部） (`cedeffd`)
- [x] Task 9: `TrashConfig`（Enabled/ConfirmBeforeMove/AutoEmptyDays/ForceFallback）、
      `sample/config.json` に反映 (`f3fe572`)
- [x] Task 10: `DeleteConfirmDialog` に解決済み `to_trash`/`force_fallback` フラグを保持 (`9762b2a`)
- [x] Task 11: `Action::Delete` が trash config を尊重、`Action::EmptyTrash` 追加 (`c9840aa`)
- [x] Task 12: `Action::DeleteForce`（Shift+Delete 物理削除）の Action 追加・ディスパッチ実装
      （`input/mod.rs` 内、Task 11 と近接コミットで導入）
- [x] Task 13: `confirm.rs` がダイアログの解決済みフラグから MoveToTrash/Delete の JobSpec を構築
      (`9ffa241`)
- [x] Task 14: `trash_job_name()` を `app.rs` の確認ハンドラから呼び出し、`JobKind::MoveToTrash`
      確定時にタスクパネルへ「Move '...' to Trash」ラベルで背景ジョブを登録。`#[allow(dead_code)]`
      を除去。TDD: 確認前は未登録（RED確認済み）→ 登録される（GREEN）をテストで固定
      (`dbe005d`)。→ UI/UX仕様 §4「視覚的フィードバック」対応完了。

ビルド確認済み: `cargo build --workspace` 成功。テスト確認済み: `cargo test -p rwf --
--test-threads=1` 237件全通過、`cargo fmt --all -- --check` / `cargo clippy --all-targets
-- -D warnings` ともにクリーン（2026-08-02）。

### 未着手・要再開（Task 15-）

- [ ] **Task 15**: `DeleteForce`（Shift+Delete）と `EmptyTrash` のデフォルトキーバインド/メニュー項目
      を追加。現状どちらも Action としてはディスパッチ可能だが、デフォルト設定・メニューJSON・
      `help_content.rs` のいずれにも未登録で、ユーザーは自分で keybindings.json を編集しない限り
      到達できない。→ UI/UX仕様 §4 の完成に必須。
- [ ] **Task 16**: ゴミ箱からの復元UI（trash 内容をブラウズして選択・復元するダイアログ）。
      `restore_from_trash_sync` / `JobKind::RestoreFromTrash` はバックエンドに存在するが、
      rwf-bin 側から呼び出す呼び出し元が無い。→ 目的節「削除済みファイルの復元」の達成に必須
      （Windows/macOSはOS標準ゴミ箱からの手動復元で代替可能だが、Linux fallback 方式
      （`.rwf-trash`）はRWF内蔵UIが無いと復元手段が無い）。
- [ ] **Task 17**: `AutoEmptyDays` の実処理。現状 config には値がパース・保持されるのみ
      (`rwf-lib/src/config.rs`)で、これを消費して実際に期限切れファイルを掃除するスケジューラ／
      起動時チェックが未実装。→ 基本仕様 §3「自動削除」に対応。
- [ ] **Task 18**: Phase 7.6 (`plan/7.6.transactional_rollback.md`) 側で `RestoreFromTrash` を
      `InverseJobSpec` として呼び出す配線。7.7 のスコープ外だが、7.6 着手前に前提が揃っている
      ことの確認としてここにも記載。

次の再開ポイントは Task 15。
